{
  "name": "@firefly-iii-mcp/cloudflare-worker",
  "version": "1.4.0",
  "description": "One-click deploy firefly-iii-mcp to Cloudflare Worker edge.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build",
    "clean": "rimraf dist",
    "deploy": "node scripts/deploy.mjs --minify",
    "cf-typegen": "wrangler types",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.3",
    "@firefly-iii-mcp/core": "1.4.0",
    "agents": "^0.0.94",
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "wrangler": "^4.20.0"
  }
}
